name = input()
count_projects = int(input())

time_per_project = 3
needed_hours = count_projects * time_per_project

result =f"The architect {name} will need {needed_hours} hours to complete {count_projects} project/s."
print(result)

