length = int(input())
width = int(input())
height = int(input())
percent = float(input()) / 100

volume_in_centimeters = length * width * height
volume_in_litters = volume_in_centimeters * 0.001 # / 1000

needed_litters = volume_in_litters * (1 - percent)

print(needed_litters)
